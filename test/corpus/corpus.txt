# Real pages from a Ricoh fi-8950, kept as the scanner sent them, with
# what paperman should make of each. They are fed sideways, as a book
# guillotined into loose sheets is, and they are the record of how
# auto-colour and the sheet-edge crop behave on the kinds of page a
# book holds, so that tuning one kind cannot quietly change another.
#
# To add a page, scan it with PAPERMAN_KIND_DEBUG set to a directory:
#
#   PAPERMAN_KIND_DEBUG=/tmp/pages ./paperman --scan --repo DIR \
#      --dir inbox --pages 2 --auto-colour --sideways right
#
# which writes the scanner's own data as pageN.jpg and prints the counts
# behind the decision. Copy the page in under a name saying what it
# holds, and add a line here.
#
# file  sideways(none|left|right)  depth(1|8|24)  height  what it holds
#
# A height of 0 asks for no check; otherwise it is the height the page
# should be stored at, and it must come out within a fiftieth of it.

cover-colour.jpg      right  24     0  a black cover printed to the edges of the sheet
title-mono.jpg        right   1  3025  a title page, a few lines of print on white
pencil-grey.jpg       right   8  3020  a printed page with a note in pencil across the head
text-unix-mono.jpg    right   1  3009  a page of printed text
text-java-mono.jpg    right   1  2845  a page of printed text, from another book

# Linux Kernel Development, scanned with the window wider and longer
# than the sheet (auto-size off, 300 x 230mm), so both edges of the
# sheet lie inside the image. The shade along those edges is what made
# every page of this book come out grey.

lkd-cover-colour.jpg      right  24  2814  a colour cover, a dark photograph over most of it
lkd-photo-grey.jpg        right   8  2796  print with a photograph of the author on it
lkd-stamp-colour.jpg      right  24  2794  a title page with a name stamp on it in blue ink
lkd-stamp-through-grey.jpg right  8  2786  the back of that sheet, the stamp showing through
lkd-copyright-mono.jpg    right   1  2774  a copyright page, small print throughout
lkd-dedication-mono.jpg   right   1  2790  a blank page with one line of print on it
lkd-contents-grey.jpg     right   8  2780  contents, set on a grey panel
lkd-chapter-grey.jpg      right   8  2787  a chapter opening, the number set in grey
lkd-figure-grey.jpg       right   8  2776  print with a figure whose boxes are filled grey

# Linux Device Drivers: a chapter opening with an engraving on it, and
# the back of the same sheet. The engraving is ink and paper with no
# mid-tones, so the rule that finds a photograph does not see it: it is
# the filled-area rule that keeps this one out of mono.

ldd-engraving-grey.jpg    right   8  2842  a chapter opening with an engraving at the head
ldd-text-mono.jpg         right   1  2834  the back of that sheet, printed text

# A page fed upright, printed on both sides, with a note written across
# the head of it in blue biro. The mark is a few thin strokes: too
# little of the page for the measure which asks what share of it has
# colour, and too thin for the one which asks for colour all round a
# pixel, so it is the pen-ink rule which keeps this one in colour.

pen-colour.jpg            none   24  2192  a printed page with a note written on it in blue ink
